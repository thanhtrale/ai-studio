# Node
node_modules/
*.log

# Nuxt / Nitro / Vite
.nuxt/
.output/
.nitro/
.cache/
dist/
.data/

# Env & secrets
.env
.env.*
!.env.example

# TLS material (không dùng nữa vì dev server chạy http; giữ để chặn lỡ tay commit key)
certs/
*.pem
*.key
*.crt
*.pfx

# Python (arms)
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.ruff_cache/
.pytest_cache/

# Arm runtimes (binaries tải/build riêng từng arm)
arms/*/bin/
arms/*/build/

# Runtime storage: models, outputs, db, cache.
# Keep the directory skeleton, ignore everything actually stored in it.
storage/**/*
!storage/**/
!storage/**/.gitkeep

# OS / editor
Thumbs.db
.DS_Store
.idea/
*.swp
